!!Clone the repo - git clone <repo_link> <br>
Install the requirements file dependencies-    pip install -r requirements.txt <br>
run the application file -  streamlit run streamlit_app.py <br>
enjoy the program !!
